<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <!-- 기존 내용 -->
    <LinearLayout
        android:id="@+id/widgetRoot"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_widget"
        android:orientation="vertical"
        android:padding="12dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/widgetImgLogo"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_alignParentStart="true"
                android:src="@drawable/ic_memong_widget_logo" />

            <ImageButton
                android:id="@+id/widgetBtnWrite"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_alignParentEnd="true"
                android:background="@null"
                android:src="@drawable/ic_widget_write" />

            <ImageButton
                android:id="@+id/widgetBtnBackup"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginEnd="8dp"
                android:layout_toStartOf="@id/widgetBtnWrite"
                android:background="@null"
                android:src="@drawable/ic_widget_backup" />
        </RelativeLayout>

        <ListView
            android:id="@+id/widgetMemoList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:background="@android:color/transparent"
            android:clipToPadding="false"
            android:divider="@android:color/transparent"
            android:dividerHeight="8dp"
            android:visibility="visible"
            android:listSelector="@android:color/transparent"
            android:paddingBottom="8dp" />
    </LinearLayout>

    <!-- 중앙 Empty 상태 텍스트 (초기엔 숨김) -->
    <TextView
        android:id="@+id/widgetTextEmpty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="@string/haru_widget_empty_text"
        android:textColor="@color/haru_primary_gray"
        android:visibility="gone" />
</FrameLayout>
