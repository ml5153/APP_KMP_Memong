<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 더 구체적인 경로를 먼저 선언 -->
    <files-path
        name="images"
        path="images/" />

    <!-- 그다음 일반적인 files 경로 -->
    <files-path
        name="files"
        path="." />

    <cache-path
        name="cache"
        path="." />

    <cache-path
        name="drawings"
        path="." />

    <external-files-path
        name="external_files"
        path="." />
</paths>
